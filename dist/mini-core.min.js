/* Mini Core v0.1.0 (2026-01-12) */(function(){'use strict';const DEFAULTS={containerSelector:'.FAQContainer',dividerSelector:'hr.divider-component',headerTags:['H1','H2','H3'],allowMultipleOpen:false,defaultOpen:false};const externalOptions=(typeof window!=='undefined'&&window.CarrdPluginOptions&&window.CarrdPluginOptions.faq)||{};const CONFIG={...DEFAULTS,...externalOptions};const HEADER_TAGS=new Set(CONFIG.headerTags);let answerIdCounter=0;const containers=document.querySelectorAll(CONFIG.containerSelector);if(!containers.length)return;const openAnswers=new Set();const requestFrame=window.requestAnimationFrame||(cb=>setTimeout(cb,16));let resizeHandle=null;const scheduleOpenAnswerSync=()=>{if(resizeHandle!==null)return;resizeHandle=requestFrame(()=>{openAnswers.forEach(answer=>adjustHeight(answer));resizeHandle=null;});};const answerResizeObserver=typeof ResizeObserver!=='undefined'?new ResizeObserver(entries=>{entries.forEach(entry=>{if(openAnswers.has(entry.target)){adjustHeight(entry.target);}});}):null;let firstQuestion=true;containers.forEach(container=>{const dividers=Array.from(container.querySelectorAll(CONFIG.dividerSelector)).filter(divider=>divider.closest(CONFIG.containerSelector)===container);if(!dividers.length)return;dividers.forEach(startDivider=>{const endDivider=findNextDivider(startDivider);const header=findHeaderBetween(startDivider,endDivider);if(!header||header.dataset.faqBound==='true'){return;}const answerWrapper=header.nextElementSibling&&header.nextElementSibling.classList.contains('faq-answer')?header.nextElementSibling:wrapAnswerContent(header,endDivider);if(!answerWrapper){return;}const shouldOpenByDefault=CONFIG.defaultOpen&&firstQuestion;prepareToggle(header,answerWrapper,shouldOpenByDefault);firstQuestion=false;});});window.addEventListener('resize',scheduleOpenAnswerSync);window.addEventListener('orientationchange',scheduleOpenAnswerSync);function findNextDivider(divider){let node=divider.nextElementSibling;while(node){if(isDivider(node)){return node;}node=node.nextElementSibling;}return null;}function findHeaderBetween(startDivider,endDivider){let node=startDivider.nextElementSibling;let firstParagraph=null;while(node&&node!==endDivider){if(isHeader(node)){return node;}if(!firstParagraph&&node.nodeType===1&&node.tagName==='P'){firstParagraph=node;}node=node.nextElementSibling;}if(!endDivider){while(node){if(isHeader(node)){return node;}if(!firstParagraph&&node.nodeType===1&&node.tagName==='P'){firstParagraph=node;}node=node.nextElementSibling;}}return firstParagraph;}function wrapAnswerContent(header,endDivider){let node=header.nextSibling;const wrapper=document.createElement('div');wrapper.className='faq-answer';let hasContent=false;while(node&&node!==endDivider){const next=node.nextSibling;wrapper.appendChild(node);hasContent=true;node=next;}if(!hasContent){return null;}if(endDivider&&endDivider.parentNode){header.parentNode.insertBefore(wrapper,endDivider);}else{header.parentNode.appendChild(wrapper);}return wrapper;}function prepareToggle(header,answer,openByDefault=false){header.classList.add('faq-question');header.dataset.faqBound='true';if(!header.hasAttribute('tabindex')){header.setAttribute('tabindex','0');}header.setAttribute('role','button');if(!answer.id){answerIdCounter+=1;answer.id=`faq-answer-${answerIdCounter}`;}header.setAttribute('aria-controls',answer.id);if(answerResizeObserver){answerResizeObserver.observe(answer);}if(openByDefault){header.classList.add('is-open');header.classList.remove('is-closed');answer.classList.add('is-open');answer.classList.remove('is-closed');header.setAttribute('aria-expanded','true');answer.setAttribute('aria-hidden','false');openAnswers.add(answer);requestFrame(()=>adjustHeight(answer));}else{header.classList.add('is-closed');header.classList.remove('is-open');answer.classList.add('is-closed');answer.classList.remove('is-open');header.setAttribute('aria-expanded','false');answer.setAttribute('aria-hidden','true');}const toggle=()=>toggleAnswer(header,answer);header.addEventListener('click',toggle);header.addEventListener('keydown',event=>{if(event.key==='Enter'||event.key===' '){event.preventDefault();toggle();}});}function toggleAnswer(header,answer){const willOpen=!header.classList.contains('is-open');if(willOpen&&!CONFIG.allowMultipleOpen){openAnswers.forEach(openAnswer=>{if(openAnswer!==answer){const openHeader=document.querySelector(`[aria-controls="${openAnswer.id}"]`);if(openHeader){openHeader.classList.remove('is-open');openHeader.classList.add('is-closed');openHeader.setAttribute('aria-expanded','false');}openAnswer.classList.remove('is-open');openAnswer.classList.add('is-closed');openAnswer.setAttribute('aria-hidden','true');openAnswer.style.maxHeight='0px';openAnswers.delete(openAnswer);}});}header.classList.toggle('is-open',willOpen);header.classList.toggle('is-closed',!willOpen);answer.classList.toggle('is-open',willOpen);answer.classList.toggle('is-closed',!willOpen);header.setAttribute('aria-expanded',String(willOpen));answer.setAttribute('aria-hidden',String(!willOpen));if(willOpen){openAnswers.add(answer);adjustHeight(answer);scheduleOpenAnswerSync();}else{openAnswers.delete(answer);answer.style.maxHeight='0px';}}function adjustHeight(answer){answer.style.maxHeight=`${answer.scrollHeight}px`;}function isHeader(node){return node&&node.nodeType===1&&HEADER_TAGS.has(node.tagName);}function isDivider(node){return(node&&node.nodeType===1&&node.tagName==='HR'&&node.classList.contains('divider-component'));}})();
(function(){'use strict';const DEFAULTS={gridClasses:['grid-2','grid-3','grid-4','grid-5','grid-6'],cardSelector:'.cards',defaultCardBg:'var(--mini-card-bg-default)',widthClasses:{'w-20':'20%','w-25':'25%','w-30':'33%','w-40':'40%','w-50':'50%','w-60':'60%','w-70':'67%','w-75':'75%','w-80':'80%'}};const externalOptions=(typeof window!=='undefined'&&window.CarrdPluginOptions&&window.CarrdPluginOptions.columns)||{};const CONFIG={...DEFAULTS,...externalOptions};const GRID_CLASSES=CONFIG.gridClasses;const WIDTH_CLASS_MAP=CONFIG.widthClasses;const GRID_SELECTOR=GRID_CLASSES.map(cls=>`.${cls}`).join(',');const WIDTH_CLASSES=Object.keys(WIDTH_CLASS_MAP);const isGridBlock=element=>element&&GRID_CLASSES.some(cls=>element.classList&&element.classList.contains(cls));const getGridSize=element=>{if(!element||!element.classList)return null;const sizeClass=GRID_CLASSES.find(cls=>element.classList.contains(cls));if(!sizeClass)return null;const numeric=parseInt(sizeClass.split('-')[1],10);return Number.isNaN(numeric)?null:numeric;};const widthValueForElement=element=>{if(!element||!element.classList)return null;const widthClass=WIDTH_CLASSES.find(cls=>element.classList.contains(cls));return widthClass?WIDTH_CLASS_MAP[widthClass]:null;};function wrapCluster(cluster,gridSize){if(!cluster.length||!cluster[0].parentNode)return;const container=document.createElement('div');const classList=['custom-grid-container'];if(gridSize&&gridSize>=2){classList.push(`grid-${gridSize}`);}if(cluster.some(el=>el.classList.contains('grid-sm-2'))){classList.push('grid-sm-2');}container.className=classList.join(' ');cluster[0].parentNode.insertBefore(container,cluster[0]);cluster.forEach(node=>container.appendChild(node));applyDesktopWidths(container,cluster,gridSize);}function applyDesktopWidths(container,cluster,gridSize){if(!gridSize||gridSize<2||cluster.length<gridSize)return;const initialRow=cluster.slice(0,gridSize);const columnWidths=initialRow.map(widthValueForElement);if(!columnWidths.some(Boolean))return;const templateParts=columnWidths.map(value=>value||'minmax(0, 1fr)');container.classList.add('custom-grid-container--desktop-widths');container.style.setProperty('--custom-desktop-template',templateParts.join(' '));}function constrainImageFrames(){document.querySelectorAll('.custom-grid-container .image-component > .frame').forEach(frame=>{const computedWidth=window.getComputedStyle(frame).width;const widthInRem=parseFloat(computedWidth)/parseFloat(getComputedStyle(document.documentElement).fontSize);if(widthInRem>20){frame.classList.add('constrain-width');}});}function initGridCluster(){const collected=new Set();const gridBlocks=document.querySelectorAll(GRID_SELECTOR);gridBlocks.forEach(block=>{if(collected.has(block))return;if(block.dataset.gridInitialized==='true')return;const cluster=[block];const baseSize=getGridSize(block);let sibling=block.nextElementSibling;while(isGridBlock(sibling)){const siblingSize=getGridSize(sibling);if(baseSize!==null&&siblingSize!==baseSize){break;}cluster.push(sibling);collected.add(sibling);sibling=sibling.nextElementSibling;}collected.add(block);cluster.forEach(node=>node.dataset.gridInitialized='true');wrapCluster(cluster,baseSize);});constrainImageFrames();}const parsePadding=(val)=>{if(!val)return null;return val.split(' ').map(v=>!isNaN(v)?v+'rem':v).join(' ');};const initCards=()=>{const cardContainers=document.querySelectorAll(CONFIG.cardSelector);cardContainers.forEach(container=>{if(container.dataset.cardsInitialized==='true')return;container.dataset.cardsInitialized='true';const style=window.getComputedStyle(container);const backgroundColor=style.backgroundColor;const borderRadius=style.borderRadius;const borderTopWidth=style.borderTopWidth;const borderTopStyle=style.borderTopStyle;const borderTopColor=style.borderTopColor;const boxShadow=style.boxShadow;const backgroundImage=style.backgroundImage;const backgroundPosition=style.backgroundPosition;const backgroundRepeat=style.backgroundRepeat;const backgroundSize=style.backgroundSize;const dataPadding=parsePadding(container.dataset.padding);const dataPaddingMobile=parsePadding(container.dataset.paddingMobile);const dataColor=container.dataset.color;container.style.setProperty('border','none','important');container.style.setProperty('box-shadow','none','important');if(!dataColor){container.style.setProperty('background-color','transparent','important');container.style.setProperty('background','none','important');}const inner=container.querySelector('.inner');if(!inner)return;const columns=Array.from(inner.children);columns.forEach((column,index)=>{if(column.querySelector('.card-item'))return;const cardItem=document.createElement('div');cardItem.classList.add('card-item');const specificColor=container.getAttribute(`data-color-${index + 1}`);const specificBorderColor=container.getAttribute(`data-border-color-${index + 1}`);if(specificColor){cardItem.style.backgroundColor=specificColor;}else if(dataColor){cardItem.style.backgroundColor=dataColor;}else if(backgroundColor&&backgroundColor!=='rgba(0, 0, 0, 0)'&&backgroundColor!=='transparent'){cardItem.style.backgroundColor=backgroundColor;}else{cardItem.style.backgroundColor=CONFIG.defaultCardBg;}if(borderRadius&&borderRadius!=='0px'){cardItem.style.setProperty('--mini-card-border-radius',borderRadius);}if(borderTopWidth&&borderTopWidth!=='0px'&&borderTopStyle!=='none'){cardItem.style.borderWidth=borderTopWidth;cardItem.style.borderStyle=borderTopStyle;cardItem.style.borderColor=specificBorderColor||borderTopColor;}if(boxShadow&&boxShadow!=='none'){cardItem.style.boxShadow=boxShadow;}if(!dataColor&&!specificColor&&backgroundImage&&backgroundImage!=='none'){cardItem.style.backgroundImage=backgroundImage;cardItem.style.backgroundPosition=backgroundPosition;cardItem.style.backgroundRepeat=backgroundRepeat;cardItem.style.backgroundSize=backgroundSize;}if(dataPadding){cardItem.style.setProperty('--mini-card-padding',dataPadding);}if(dataPaddingMobile){cardItem.style.setProperty('--mini-card-padding-mobile',dataPaddingMobile);}while(column.firstChild){cardItem.appendChild(column.firstChild);}column.appendChild(cardItem);});});};function init(){initGridCluster();initCards();}if(document.readyState==='loading'){document.addEventListener('DOMContentLoaded',init);}else{init();}})();
(function(){'use strict';const DEFAULTS={currency:'$',currencyPosition:'before',position:'top-right',storageKey:'carrd_cart_v1',orderInputSelector:'[name="order-details"], #order-details',orderInputClass:'.cart-output',texts:{title:'Shopping Cart',empty:'Your cart is empty.',checkout:'Checkout',total:'Total',remove:'Remove',required:'Required',addedToCart:'Added "${name}" to cart',errorName:'Invalid product name',errorPrice:'Invalid price for ${name}',errorForm:'Error: Could not find the order form. Please contact support.',consoleErrorForm:'Carrd Cart: Could not find "Order Details" field. Please ensure a Textarea with name="order-details" or ID="order-details" exists.'}};function deepMerge(target,source){if(!source||typeof source!=='object')return target;const result={...target};for(const key in source){if(Object.prototype.hasOwnProperty.call(source,key)){if(source[key]&&typeof source[key]==='object'&&!Array.isArray(source[key])){result[key]=deepMerge(target[key]||{},source[key]);}else{result[key]=source[key];}}}return result;}const externalOptions=(typeof window!=='undefined'&&window.CarrdPluginOptions&&window.CarrdPluginOptions.shoppingCart)||{};const CONFIG=deepMerge(DEFAULTS,externalOptions);function escapeHtml(str){if(typeof str!=='string')return'';const div=document.createElement('div');div.textContent=str;return div.innerHTML;}function validateCartItem(item){if(!item||typeof item!=='object')return false;if(typeof item.name!=='string'||item.name.length===0||item.name.length>200)return false;if(typeof item.price!=='number'||isNaN(item.price)||item.price<0)return false;if(typeof item.qty!=='number'||!Number.isInteger(item.qty)||item.qty<1)return false;return true;}let state={cart:[],isOpen:false};try{const stored=localStorage.getItem(CONFIG.storageKey);if(stored){const parsed=JSON.parse(stored);if(Array.isArray(parsed)){state.cart=parsed.filter(validateCartItem);}}}catch(e){console.warn('LocalStorage not available or corrupted');state.cart=[];}const saveState=()=>{try{localStorage.setItem(CONFIG.storageKey,JSON.stringify(state.cart));}catch(e){}updateUI();};const CartAPI={add:function(name,price){if(typeof name!=='string'||name.trim().length===0){console.error(CONFIG.texts.errorName);return;}name=name.trim();if(name.length>200){console.error('Product name too long (max 200 chars)');return;}price=parseFloat(price);if(isNaN(price)||price<0){console.error(CONFIG.texts.errorPrice.replace('${name}',name));return;}const existing=state.cart.find(item=>item.name===name);if(existing){existing.qty++;}else{state.cart.push({name,price,qty:1});}saveState();showToast(CONFIG.texts.addedToCart.replace('${name}',name));if(state.cart.length===1&&state.cart[0].qty===1){this.open();}},remove:function(name){state.cart=state.cart.filter(item=>item.name!==name);saveState();},updateQty:function(name,delta){const item=state.cart.find(i=>i.name===name);if(!item)return;item.qty+=delta;if(item.qty<=0){this.remove(name);}else{saveState();}},clear:function(){state.cart=[];saveState();},getCart:function(){return[...state.cart];},getTotal:function(){return state.cart.reduce((sum,item)=>sum+(item.price*item.qty),0);},open:function(){state.isOpen=true;updateUI();},close:function(){state.isOpen=false;updateUI();},checkout:function(){if(state.cart.length===0)return;const total=this.getTotal();const summary=['--- ORDER DETAILS ---',...state.cart.map(item=>`${item.qty} x ${item.name}: ${formatPrice(item.price * item.qty)}`),'---------------------',`TOTAL: ${formatPrice(total)}`].join('\n');let orderField=document.querySelector(CONFIG.orderInputSelector)||document.querySelector(CONFIG.orderInputClass)||document.querySelector('[name="order-details"]')||document.querySelector('#order-details');if(!orderField){const candidates=document.querySelectorAll('textarea');for(const el of candidates){const id=(el.id||'').toLowerCase();const name=(el.name||'').toLowerCase();if(id.includes('order')||name.includes('order')||id.includes('cart')||name.includes('cart')){orderField=el;console.log('Carrd Cart: Found order field via fuzzy search:',el);break;}}}if(!orderField){console.error(CONFIG.texts.consoleErrorForm);alert(CONFIG.texts.errorForm);return;}orderField.value=summary;this.close();window.location.href='#shopping-cart';},configure:function(options){if(!options||typeof options!=='object')return;const merged=deepMerge(CONFIG,options);Object.assign(CONFIG,merged);updateUI();},getConfig:function(){return JSON.parse(JSON.stringify(CONFIG));}};window.CartPlugin=CartAPI;window.CarrdCart=CartAPI;const ICONS={cart:`<svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="9" cy="21" r="1"></circle><circle cx="20" cy="21" r="1"></circle><path d="M1 1h4l2.68 13.39a2 2 0 0 0 2 1.61h9.72a2 2 0 0 0 2-1.61L23 6H6"></path></svg>`,close:`<svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="18" y1="6" x2="6" y2="18"></line><line x1="6" y1="6" x2="18" y2="18"></line></svg>`,trash:`<svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="3 6 5 6 21 6"></polyline><path d="M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6m3 0V4a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v2"></path></svg>`,plus:`<svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="12" y1="5" x2="12" y2="19"></line><line x1="5" y1="12" x2="19" y2="12"></line></svg>`,minus:`<svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="5" y1="12" x2="19" y2="12"></line></svg>`};function formatPrice(amount){const fixed=amount.toFixed(2);return CONFIG.currencyPosition==='before'?`${CONFIG.currency}${fixed}`:`${fixed}${CONFIG.currency}`;}function renderCartItems(){if(state.cart.length===0){return`<div style="text-align: center; padding: 40px 0; opacity: 0.6;">${escapeHtml(CONFIG.texts.empty)}</div>`;}return state.cart.map(item=>{const safeName=escapeHtml(item.name);return`
            <div class="crt-item">
                <div class="crt-item-info">
                    <span class="crt-item-name">${safeName}</span>
                    <span class="crt-item-price">${formatPrice(item.price)}</span>
                </div>
                <div class="crt-controls">
                    <button class="crt-btn-qty" data-action="update-qty" data-name="${safeName}" data-qty="-1" aria-label="Decrease quantity for ${safeName}">${ICONS.minus}</button>
                    <span>${item.qty}</span>
                    <button class="crt-btn-qty" data-action="update-qty" data-name="${safeName}" data-qty="1" aria-label="Increase quantity for ${safeName}">${ICONS.plus}</button>
                    <button class="crt-btn-qty crt-btn-remove" style="margin-left:5px" data-action="remove" data-name="${safeName}" aria-label="Remove ${safeName} from cart">${ICONS.trash}</button>
                </div>
            </div>
        `;}).join('');}function updateUI(){const container=document.getElementById('crt-container');if(!container)return;const widget=container.querySelector('.crt-widget');const badge=container.querySelector('.crt-badge');const totalQty=state.cart.reduce((s,i)=>s+i.qty,0);if(state.cart.length>0){widget.classList.add('visible');}else{widget.classList.remove('visible');}if(badge){badge.textContent=totalQty;badge.style.display=totalQty>0?'block':'none';}const overlay=container.querySelector('.crt-overlay');const panel=container.querySelector('.crt-panel');if(state.isOpen){overlay.classList.add('open');panel.classList.add('open');}else{overlay.classList.remove('open');panel.classList.remove('open');}const bodyContent=container.querySelector('.crt-body');const footerContent=container.querySelector('.crt-footer');const titleContent=container.querySelector('.crt-title');titleContent.textContent=CONFIG.texts.title;bodyContent.innerHTML=renderCartItems();footerContent.style.display='block';container.querySelector('.crt-total-amount').textContent=formatPrice(CartAPI.getTotal());const checkoutBtn=container.querySelector('.crt-btn-checkout');if(checkoutBtn){checkoutBtn.disabled=state.cart.length===0;}}function createWidget(){const div=document.createElement('div');div.id='crt-container';const posMap={'bottom-right':'crt-pos-br','top-right':'crt-pos-tr','bottom-left':'crt-pos-bl','bottom-center':'crt-pos-bc'};const posClass=posMap[CONFIG.position]||'crt-pos-br';div.innerHTML=`
            <div class="crt-widget ${posClass}" data-action="open" role="button" aria-label="Open Shopping Cart" tabindex="0">
                ${ICONS.cart}
                <div class="crt-badge" style="display:none">0</div>
            </div>
            <div class="crt-overlay" data-action="close"></div>
            <div class="crt-panel">
                <div class="crt-header">
                    <h2 class="crt-title">${CONFIG.texts.title}</h2>
                    <button class="crt-close" data-action="close" aria-label="Close Cart">${ICONS.close}</button>
                </div>
                <div class="crt-body"></div>
                <div class="crt-footer">
                    <div class="crt-total-row">
                        <span>${CONFIG.texts.total}</span>
                        <span class="crt-total-amount">$0.00</span>
                    </div>
                    <button class="crt-btn-main crt-btn-checkout" data-action="checkout">${CONFIG.texts.checkout}</button>
                </div>
            </div>
            <div class="crt-toast"></div>
        `;document.body.appendChild(div);div.addEventListener('click',(e)=>{const trigger=e.target.closest('[data-action]');if(!trigger)return;const action=trigger.dataset.action;const name=trigger.dataset.name;if(trigger.tagName==='BUTTON')e.preventDefault();switch(action){case'open':CartAPI.open();break;case'close':CartAPI.close();break;case'checkout':CartAPI.checkout();break;case'update-qty':{const delta=parseInt(trigger.dataset.qty);if(name&&!isNaN(delta))CartAPI.updateQty(name,delta);break;}case'remove':if(name)CartAPI.remove(name);break;}});div.addEventListener('keydown',(e)=>{if(e.key==='Enter'||e.key===' '){const trigger=e.target.closest('[data-action="open"]');if(trigger){e.preventDefault();CartAPI.open();}}});}function showToast(msg){const toast=document.querySelector('.crt-toast');if(!toast)return;toast.textContent=msg;toast.classList.add('visible');setTimeout(()=>toast.classList.remove('visible'),3000);}function init(){if(document.getElementById('crt-container'))return;createWidget();updateUI();}if(document.readyState==='loading'){document.addEventListener('DOMContentLoaded',init);}else{init();}})();
(function(){'use strict';const DEFAULTS={showDots:true,showArrows:true,loop:false,autoplay:false,autoplayInterval:5000,dragThreshold:50,snapThreshold:0.3,gap:16,slidesPerView:1,peek:0,equalHeight:true,breakpoints:{737:{slidesPerView:3},1280:{slidesPerView:4}}};const externalOptions=(typeof window!=='undefined'&&window.CarrdPluginOptions&&window.CarrdPluginOptions.slider)||{};const BASE_CONFIG={...DEFAULTS,...externalOptions,breakpoints:{...DEFAULTS.breakpoints,...(externalOptions.breakpoints||{})}};const INSTANCE_CONFIGS=externalOptions.instances||{};const SELECTORS={slideSelector:'.slider',wrapperClass:'slider-wrapper',trackClass:'slider-track',slideClass:'slider-slide'};const ICONS={prev:`<svg viewBox="0 0 24 24"><polyline points="15 18 9 12 15 6"></polyline></svg>`,next:`<svg viewBox="0 0 24 24"><polyline points="9 6 15 12 9 18"></polyline></svg>`};const SLIDER_INSTANCES=[];class Slider{constructor(slides,config){this.slides=slides;this.config=config;this.currentIndex=0;this.isDragging=false;this.startX=0;this.currentX=0;this.translateX=0;this.autoplayTimer=null;this.slidesPerView=this.config.slidesPerView;this.eventHandlers=[];this.dotHandlers=[];this.resizeTimeout=null;this.init();}init(){this.createWrapper();this.createTrack();this.wrapSlides();if(this.config.showDots&&this.slides.length>1){this.createDots();}if(this.config.showArrows&&this.slides.length>1){this.createArrows();}this.bindEvents();this.updateSlidesPerView();this.updateSlider();if(this.config.autoplay&&this.slides.length>1){this.startAutoplay();}}createWrapper(){this.wrapper=document.createElement('div');this.wrapper.className=SELECTORS.wrapperClass;if(this.config.equalHeight){this.wrapper.classList.add('is-equal-height');}this.slides[0].parentNode.insertBefore(this.wrapper,this.slides[0]);}createTrack(){this.track=document.createElement('div');this.track.className=SELECTORS.trackClass;this.wrapper.appendChild(this.track);}wrapSlides(){this.slides.forEach((slide,index)=>{const slideWrapper=document.createElement('div');slideWrapper.className=SELECTORS.slideClass;slideWrapper.dataset.slideIndex=index;slideWrapper.appendChild(slide);this.track.appendChild(slideWrapper);});this.slideElements=Array.from(this.track.querySelectorAll(`.${SELECTORS.slideClass}`));}createDots(){this.dotsContainer=document.createElement('div');this.dotsContainer.className='slider-dots';this.wrapper.appendChild(this.dotsContainer);}updateDots(){if(!this.dotsContainer)return;this.clearDotListeners();this.dotsContainer.innerHTML='';const totalPages=this.getTotalPages();if(totalPages<=1){this.dotsContainer.style.display='none';return;}this.dotsContainer.style.display='flex';for(let i=0;i<totalPages;i++){const dot=document.createElement('button');dot.className='slider-dot';if(i===this.currentIndex){dot.classList.add('is-active');}dot.setAttribute('aria-label',`Go to slide ${i + 1}`);dot.dataset.index=i;const handler=()=>this.goToSlide(i);dot.addEventListener('click',handler);this.dotHandlers.push({element:dot,handler});this.dotsContainer.appendChild(dot);}this.dots=this.dotsContainer.querySelectorAll('.slider-dot');}createArrows(){this.prevBtn=document.createElement('button');this.prevBtn.className='slider-nav slider-nav--prev';this.prevBtn.setAttribute('aria-label','Previous slide');this.prevBtn.innerHTML=ICONS.prev;this.prevHandler=()=>this.prev();this.addListener(this.prevBtn,'click',this.prevHandler);this.nextBtn=document.createElement('button');this.nextBtn.className='slider-nav slider-nav--next';this.nextBtn.setAttribute('aria-label','Next slide');this.nextBtn.innerHTML=ICONS.next;this.nextHandler=()=>this.next();this.addListener(this.nextBtn,'click',this.nextHandler);this.wrapper.appendChild(this.prevBtn);this.wrapper.appendChild(this.nextBtn);}bindEvents(){const dragTarget=this.wrapper;this.onDragStartHandler=this.onDragStart.bind(this);this.onDragMoveHandler=this.onDragMove.bind(this);this.onDragEndHandler=this.onDragEnd.bind(this);this.onDragPreventHandler=(e)=>e.preventDefault();this.addListener(dragTarget,'touchstart',this.onDragStartHandler,{passive:true,capture:true});this.addListener(dragTarget,'touchmove',this.onDragMoveHandler,{passive:false,capture:true});this.addListener(dragTarget,'touchend',this.onDragEndHandler,{capture:true});this.addListener(dragTarget,'touchcancel',this.onDragEndHandler,{capture:true});this.addListener(dragTarget,'mousedown',this.onDragStartHandler,{capture:true});this.addListener(dragTarget,'mousemove',this.onDragMoveHandler,{capture:true});this.addListener(dragTarget,'mouseup',this.onDragEndHandler,{capture:true});this.addListener(dragTarget,'mouseleave',this.onDragEndHandler,{capture:true});this.addListener(dragTarget,'dragstart',this.onDragPreventHandler);this.wrapper.setAttribute('tabindex','0');this.onKeydownHandler=(e)=>{if(e.key==='ArrowLeft')this.prev();if(e.key==='ArrowRight')this.next();};this.addListener(this.wrapper,'keydown',this.onKeydownHandler);if(this.config.autoplay){this.onMouseEnterHandler=()=>this.stopAutoplay();this.onMouseLeaveHandler=()=>this.startAutoplay();this.addListener(this.wrapper,'mouseenter',this.onMouseEnterHandler);this.addListener(this.wrapper,'mouseleave',this.onMouseLeaveHandler);}this.onResizeHandler=()=>{clearTimeout(this.resizeTimeout);this.resizeTimeout=setTimeout(()=>{this.updateSlidesPerView();this.updateSlider();},100);};this.addListener(window,'resize',this.onResizeHandler);}addListener(target,event,handler,options){target.addEventListener(event,handler,options);this.eventHandlers.push({target,event,handler,options});}clearDotListeners(){this.dotHandlers.forEach(({element,handler})=>{element.removeEventListener('click',handler);});this.dotHandlers=[];}destroy(){this.stopAutoplay();this.clearDotListeners();this.eventHandlers.forEach(({target,event,handler,options})=>{target.removeEventListener(event,handler,options);});this.eventHandlers=[];clearTimeout(this.resizeTimeout);this.resizeTimeout=null;}updateSlidesPerView(){const windowWidth=window.innerWidth;let newSlidesPerView=this.config.slidesPerView;const breakpointKeys=Object.keys(this.config.breakpoints).map(Number).sort((a,b)=>a-b);for(const bp of breakpointKeys){if(windowWidth>=bp){newSlidesPerView=this.config.breakpoints[bp].slidesPerView||newSlidesPerView;}}if(this.config.peek){newSlidesPerView+=this.config.peek;}this.slidesPerView=Math.min(newSlidesPerView,this.slides.length);this.updateSlideWidths();this.updateDots();const maxIndex=this.getTotalPages()-1;if(this.currentIndex>maxIndex){this.currentIndex=Math.max(0,maxIndex);}}updateSlideWidths(){const gap=this.config.gap;const totalGaps=Math.ceil(this.slidesPerView)-1;const slideWidth=`calc((100% - ${totalGaps * gap}px) / ${this.slidesPerView})`;this.slideElements.forEach((slide,index)=>{slide.style.flex=`0 0 ${slideWidth}`;slide.style.width=slideWidth;slide.style.marginRight=index<this.slides.length-1?`${gap}px`:'0';});}getTotalPages(){return Math.ceil(Math.max(1,this.slides.length-this.slidesPerView+1));}onDragStart(e){if(this.slides.length<=1)return;this.isDragging=true;this.wrapper.classList.add('is-dragging');this.startX=this.getPositionX(e);this.currentX=this.startX;this.stopAutoplay();}onDragMove(e){if(!this.isDragging)return;this.currentX=this.getPositionX(e);const diff=this.currentX-this.startX;let newTranslate=this.translateX+diff;const maxTranslate=0;const minTranslate=this.getMinTranslate();if(newTranslate>maxTranslate){newTranslate=maxTranslate+(diff*0.2);}else if(newTranslate<minTranslate){newTranslate=minTranslate+((newTranslate-minTranslate)*0.2);}this.track.style.transform=`translateX(${newTranslate}px)`;if(e.cancelable&&Math.abs(diff)>10){e.preventDefault();}}onDragEnd(){if(!this.isDragging)return;this.isDragging=false;this.wrapper.classList.remove('is-dragging');const diff=this.currentX-this.startX;const slideWidth=this.getSlideWidth();const threshold=slideWidth*this.config.snapThreshold;if(Math.abs(diff)>threshold){if(diff>0){this.prev();}else{this.next();}}else{this.updateSlider();}if(this.config.autoplay){this.startAutoplay();}}getPositionX(e){if(e.type.includes('touch')){const touch=(e.touches&&e.touches[0])||(e.changedTouches&&e.changedTouches[0]);return touch?touch.clientX:0;}return e.clientX;}getSlideWidth(){if(!this.slideElements||!this.slideElements[0])return 0;return this.slideElements[0].offsetWidth+this.config.gap;}getMinTranslate(){const slideWidth=this.getSlideWidth();return-(slideWidth*(this.getTotalPages()-1));}goToSlide(index){const maxIndex=this.getTotalPages()-1;if(index<0){index=this.config.loop?maxIndex:0;}else if(index>maxIndex){index=this.config.loop?0:maxIndex;}this.currentIndex=index;this.updateSlider();}prev(){this.goToSlide(this.currentIndex-1);}next(){this.goToSlide(this.currentIndex+1);}updateSlider(){const slideWidth=this.getSlideWidth();this.translateX=-slideWidth*this.currentIndex;this.track.style.transform=`translateX(${this.translateX}px)`;if(this.dots){this.dots.forEach((dot,index)=>{dot.classList.toggle('is-active',index===this.currentIndex);});}if(this.prevBtn&&this.nextBtn&&!this.config.loop){this.prevBtn.disabled=this.currentIndex===0;this.nextBtn.disabled=this.currentIndex>=this.getTotalPages()-1;}}startAutoplay(){this.stopAutoplay();this.autoplayTimer=setInterval(()=>{this.next();},this.config.autoplayInterval);}stopAutoplay(){if(this.autoplayTimer){clearInterval(this.autoplayTimer);this.autoplayTimer=null;}}}function findSliderClusters(){const allSliders=document.querySelectorAll(SELECTORS.slideSelector);if(!allSliders.length)return[];const clusters=[];const processed=new Set();allSliders.forEach(slide=>{if(processed.has(slide))return;if(slide.dataset.sliderInitialized==='true')return;const cluster=[slide];processed.add(slide);let sibling=slide.nextElementSibling;while(sibling&&sibling.classList.contains('slider')){if(processed.has(sibling))break;cluster.push(sibling);processed.add(sibling);sibling=sibling.nextElementSibling;}cluster.forEach(el=>el.dataset.sliderInitialized='true');clusters.push(cluster);});return clusters;}function buildInstanceConfig(instanceId){const instanceOptions=(instanceId&&INSTANCE_CONFIGS[instanceId])||{};return{...BASE_CONFIG,...instanceOptions,breakpoints:{...BASE_CONFIG.breakpoints,...(instanceOptions.breakpoints||{})}};}function registerInstance(instance,slides,instanceId){SLIDER_INSTANCES.push({instance,slides,instanceId});}function destroyById(instanceId){const index=SLIDER_INSTANCES.findIndex(entry=>entry.instanceId===instanceId);if(index===-1)return false;SLIDER_INSTANCES[index].instance.destroy();SLIDER_INSTANCES.splice(index,1);return true;}function destroyAll(){SLIDER_INSTANCES.forEach(entry=>entry.instance.destroy());SLIDER_INSTANCES.length=0;}function getInstances(){return SLIDER_INSTANCES.map(entry=>entry.instance);}function init(){const clusters=findSliderClusters();clusters.forEach(cluster=>{if(cluster.length>=1){const instanceId=cluster[0].dataset.sliderId||'';const instanceConfig=buildInstanceConfig(instanceId);const instance=new Slider(cluster,instanceConfig);registerInstance(instance,cluster,instanceId);}});}window.CarrdSlider={init,destroyAll,destroyById,getInstances};if(document.readyState==='loading'){document.addEventListener('DOMContentLoaded',init);}else{init();}})();
(function(){'use strict';const DEFAULTS={modalSelector:'.container-component.modal',closeOnOverlay:true,closeOnEscape:true,showCloseButton:true,lockBodyScroll:true,preventWhenCartOpen:false};const externalOptions=(typeof window!=='undefined'&&window.CarrdPluginOptions&&window.CarrdPluginOptions.modal)||{};const CONFIG={...DEFAULTS,...externalOptions};const ICONS={close:`<svg viewBox="0 0 24 24"><line x1="18" y1="6" x2="6" y2="18"></line><line x1="6" y1="6" x2="18" y2="18"></line></svg>`};let activeModal=null;let overlay=null;const modalWrappers=new Map();const cartOpenSelector='.crt-panel.open, .crt-overlay.open';const ModalAPI={open:function(modalId){if(CONFIG.preventWhenCartOpen&&document.querySelector(cartOpenSelector)){return;}const id=modalId.replace(/^#/,'');const modal=modalWrappers.get(id);if(!modal){console.warn(`Modal: No modal found with id "${id}"`);return;}if(activeModal&&activeModal!==id){this.close();}this.lastFocus=document.activeElement;ensureOverlayPlacement(modal);if(overlay){overlay.classList.add('is-open');}modal.classList.add('is-open');modal.setAttribute('aria-hidden','false');requestAnimationFrame(()=>{requestAnimationFrame(()=>{modal.classList.add('is-visible');});});if(CONFIG.lockBodyScroll){document.body.classList.add('modal-open');}activeModal=id;const firstFocusable=modal.querySelector('button, [href], input, select, textarea, [tabindex]:not([tabindex="-1"])');if(firstFocusable){setTimeout(()=>firstFocusable.focus(),50);}else{modal.setAttribute('tabindex','-1');modal.focus();}document.addEventListener('keydown',this.handleTabKey);},handleTabKey:function(e){if(e.key!=='Tab'||!activeModal)return;const modal=modalWrappers.get(activeModal);const focusables=modal.querySelectorAll('button, [href], input, select, textarea, [tabindex]:not([tabindex="-1"])');if(focusables.length===0){e.preventDefault();return;}const first=focusables[0];const last=focusables[focusables.length-1];if(e.shiftKey){if(document.activeElement===first){e.preventDefault();last.focus();}}else{if(document.activeElement===last){e.preventDefault();first.focus();}}},close:function(){if(!activeModal)return;const modal=modalWrappers.get(activeModal);document.removeEventListener('keydown',this.handleTabKey);if(overlay){overlay.classList.remove('is-open');}if(modal){modal.classList.remove('is-visible');modal.classList.remove('is-open');modal.setAttribute('aria-hidden','true');}if(CONFIG.lockBodyScroll){document.body.classList.remove('modal-open');}activeModal=null;if(this.lastFocus&&typeof this.lastFocus.focus==='function'){this.lastFocus.focus();this.lastFocus=null;}},toggle:function(modalId){const id=modalId.replace(/^#/,'');if(activeModal===id){this.close();}else{this.open(id);}},isOpen:function(modalId){if(modalId){return activeModal===modalId.replace(/^#/,'');}return activeModal!==null;}};window.ModalPlugin=ModalAPI;window.CarrdModal=ModalAPI;function createOverlay(){overlay=document.createElement('div');overlay.className='modal-overlay';overlay.setAttribute('aria-hidden','true');if(CONFIG.closeOnOverlay){overlay.addEventListener('click',()=>ModalAPI.close());}document.body.appendChild(overlay);}function ensureOverlayPlacement(modal){if(!overlay||!modal||!modal.parentNode)return;const parent=modal.parentNode;if(overlay.parentNode!==parent||overlay.nextSibling!==modal){parent.insertBefore(overlay,modal);}}function setupModal(modal){const modalId=modal.id;if(!modalId){console.warn('Modal: Modal element must have an ID',modal);return null;}if(CONFIG.showCloseButton){if(!modal.querySelector('.modal-close')){const closeBtn=document.createElement('button');closeBtn.className='modal-close';closeBtn.setAttribute('aria-label','Close modal');closeBtn.innerHTML=ICONS.close;closeBtn.addEventListener('click',(e)=>{e.stopPropagation();ModalAPI.close();});const inner=modal.querySelector('.inner');if(inner){inner.insertBefore(closeBtn,inner.firstChild);}else{modal.insertBefore(closeBtn,modal.firstChild);}}}modal.setAttribute('role','dialog');modal.setAttribute('aria-modal','true');modal.setAttribute('aria-hidden','true');modalWrappers.set(modalId,modal);modal.dataset.modalInitialized='true';return modal;}function bindTriggers(){document.addEventListener('click',(e)=>{const trigger=e.target.closest('a[href^="#"], button[data-modal]');if(!trigger)return;let modalId=null;if(trigger.hasAttribute('href')){const href=trigger.getAttribute('href');if(href&&href.startsWith('#')&&href.length>1){const targetId=href.substring(1);if(modalWrappers.has(targetId)){modalId=targetId;}}}if(!modalId&&trigger.hasAttribute('data-modal')){modalId=trigger.getAttribute('data-modal').replace(/^#/,'');}if(modalId){e.preventDefault();ModalAPI.open(modalId);}});}function bindKeyboard(){if(CONFIG.closeOnEscape){document.addEventListener('keydown',(e)=>{if(e.key==='Escape'&&activeModal){ModalAPI.close();}});}}function init(){const modals=document.querySelectorAll(CONFIG.modalSelector);if(!modals.length)return;createOverlay();modals.forEach(modal=>{if(modal.dataset.modalInitialized==='true')return;setupModal(modal);});bindTriggers();bindKeyboard();}if(document.readyState==='loading'){document.addEventListener('DOMContentLoaded',init);}else{init();}})();