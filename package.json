{
  "name": "carrd-custom",
  "version": "1.0.0",
  "description": "Carrd customizations with automatic CDN deployment via jsDelivr",
  "scripts": {
    "build": "node .github/scripts/build.js",
    "validate": "node .github/scripts/validate.js",
    "dev": "npm run validate && npm run build",
    "test:minifiers": "node tests/minify.test.js"
  },
  "keywords": ["carrd", "customization", "cdn", "jsdelivr"],
  "author": "popskraft",
  "license": "MIT",
  "engines": {
    "node": ">=14.0.0"
  }
}
