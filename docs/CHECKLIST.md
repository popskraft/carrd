# Полный чек-лист внедрения

## ✅ Этап 1: Подготовка

- [x] Новая структура `src/` создана
- [x] Build скрипты написаны
- [x] GitHub Actions workflow настроен
- [x] Документация подготовлена
- [x] Package.json обновлен

## 📋 Этап 2: Первичная настройка (выполнить один раз)

### 2.1 Локальная установка
```bash
cd /Users/popskraft/projects/carrd
npm install
```
- [ ] Зависимости установлены
- [ ] Нет ошибок в консоли

### 2.2 Валидация структуры
```bash
npm run validate
```
- [ ] Вывод: `✓ Validation passed!`
- [ ] Нет ошибок

### 2.3 Локальный build
```bash
npm run build
```
- [ ] Вывод: `✨ Build complete!`
- [ ] Созданы файлы в `dist/mini/`
- [ ] Созданы `dist/manifest.json`
- [ ] Созданы `docs/CDN-LINKS.md`

### 2.4 Проверка результатов
```bash
# Проверить структуру dist
ls -la dist/mini/
# Должны быть: grid-cluster.css, grid-cluster.js

# Проверить manifest
cat dist/manifest.json
# Должен содержать CDN ссылки

# Проверить CDN ссылки
cat docs/CDN-LINKS.md
# Должны быть готовые ссылки для копирования
```
- [ ] `dist/mini/grid-cluster.css` существует
- [ ] `dist/mini/grid-cluster.js` существует
- [ ] `dist/manifest.json` содержит CDN ссылки
- [ ] `docs/CDN-LINKS.md` готов к использованию

### 2.5 Коммит и пуш
```bash
git add .
git commit -m "Setup: New CDN deployment architecture"
git push origin main
```
- [ ] Все файлы добавлены
- [ ] Коммит успешен
- [ ] Push выполнен

### 2.6 GitHub Actions
1. Откройте https://github.com/popskraft/carrd/actions
2. Дождитесь завершения `Build and Deploy to CDN`
3. Проверьте логи

- [ ] Workflow запустился
- [ ] Валидация прошла
- [ ] Build завершился
- [ ] Нет ошибок в логах
- [ ] `dist/` обновлен
- [ ] `docs/CDN-LINKS.md` обновлен

## 🎯 Этап 3: Использование в Carrd

### 3.1 Получить CDN ссылки
```bash
cat docs/CDN-LINKS.md
```
- [ ] Скопированы CSS ссылки
- [ ] Скопированы JS ссылки

### 3.2 Вставить в Carrd
1. Откройте Carrd сайт
2. Перейдите в Settings → Custom Code
3. Вставьте:
```html
<link rel="stylesheet" href="https://cdn.jsdelivr.net/gh/popskraft/carrd@main/dist/mini/grid-cluster.css">
<script src="https://cdn.jsdelivr.net/gh/popskraft/carrd@main/dist/mini/grid-cluster.js"></script>
```
4. Сохраните

- [ ] CSS ссылка добавлена
- [ ] JS ссылка добавлена
- [ ] Сохранено в Carrd
- [ ] Кастомизация работает на сайте

## 🔄 Этап 4: Рабочий процесс

### 4.1 Редактирование кода

Когда нужно обновить кастомизацию:

```bash
# 1. Отредактируйте файлы
nano src/mini/grid-cluster/style.css
nano src/mini/grid-cluster/script.js

# 2. Локально протестируйте
npm run build

# 3. Проверьте результаты
cat dist/mini/grid-cluster.css
cat dist/mini/grid-cluster.js

# 4. Коммитите и пушите
git add src/
git commit -m "Update: grid-cluster improvements"
git push origin main
```

- [ ] Код отредактирован
- [ ] Локальный build успешен
- [ ] Результаты проверены
- [ ] Коммит и пуш выполнены

### 4.2 GitHub Action обновит CDN

После пуша GitHub Action автоматически:
- Валидирует структуру
- Минифицирует код
- Обновляет `dist/`
- Генерирует новые CDN ссылки

- [ ] GitHub Action запустился
- [ ] Build завершился
- [ ] `dist/` обновлен
- [ ] CDN ссылки актуальны

### 4.3 CDN обновится

jsDelivr кэширует файлы в течение нескольких минут.

- [ ] Дождались обновления CDN
- [ ] Новый код работает на сайте

## ➕ Этап 5: Добавление новой кастомизации

Когда нужна новая кастомизация:

### 5.1 Создать структуру
```bash
mkdir -p src/mini/smooth-scroll
touch src/mini/smooth-scroll/{index.html,style.css,script.js,meta.json}
```
- [ ] Папка создана
- [ ] Файлы созданы

### 5.2 Заполнить файлы

**index.html** — описание (опционально)
**style.css** — стили
**script.js** — скрипты
**meta.json** — метаданные

- [ ] `index.html` заполнен
- [ ] `style.css` заполнен
- [ ] `script.js` заполнен
- [ ] `meta.json` заполнен

### 5.3 Обновить index.json

В `src/mini/index.json` добавить:
```json
{
  "id": "smooth-scroll",
  "path": "smooth-scroll",
  "enabled": true
}
```
- [ ] `index.json` обновлен

### 5.4 Валидировать и build
```bash
npm run validate
npm run build
```
- [ ] Валидация прошла
- [ ] Build успешен
- [ ] Новые файлы в `dist/`

### 5.5 Коммит и пуш
```bash
git add src/
git commit -m "Add: smooth-scroll customization"
git push origin main
```
- [ ] Коммит и пуш выполнены
- [ ] GitHub Action запустился

### 5.6 Использовать новую кастомизацию
```bash
cat docs/CDN-LINKS.md
# Скопировать новые ссылки
```
- [ ] Новые CDN ссылки получены
- [ ] Вставлены в Carrd
- [ ] Работают на сайте

## 🐛 Этап 6: Решение проблем

### Проблема: "Missing meta.json"
```bash
# Проверить структуру
ls -la src/mini/grid-cluster/
# Должны быть: index.html, style.css, script.js, meta.json
```
- [ ] Все файлы присутствуют

### Проблема: "Missing CSS/JS file"
```bash
# Проверить пути в meta.json
cat src/mini/grid-cluster/meta.json
# Убедиться, что files.css и files.js указывают на существующие файлы
```
- [ ] Пути в meta.json корректны

### Проблема: CDN ссылки не обновляются
1. Дождитесь завершения GitHub Action
2. Очистите кэш: добавьте `?v=1.0.1` к ссылке
3. Проверьте `docs/CDN-LINKS.md`
- [ ] GitHub Action завершился
- [ ] Кэш очищен
- [ ] CDN ссылки обновлены

### Проблема: GitHub Action не запускается
1. Проверьте `.github/workflows/build-and-deploy.yml`
2. Убедитесь, что файлы в `src/` изменились
3. Проверьте логи в Actions
- [ ] Workflow файл корректен
- [ ] Файлы в `src/` изменены
- [ ] Логи проверены

## 📊 Этап 7: Мониторинг

### Регулярно проверяйте:
- [ ] GitHub Actions логи (нет ошибок)
- [ ] `dist/` папка (файлы обновляются)
- [ ] `docs/CDN-LINKS.md` (ссылки актуальны)
- [ ] CDN ссылки в Carrd (работают)

### Версионирование:
- [ ] Обновляйте версию в `meta.json` при изменениях
- [ ] Ведите `changelog` в `meta.json`
- [ ] Коммитьте с понятными сообщениями

## 🎉 Финальная проверка

```bash
# 1. Валидация
npm run validate
# Результат: ✓ Validation passed!

# 2. Build
npm run build
# Результат: ✨ Build complete!

# 3. Проверить dist
ls -la dist/mini/
# Результат: grid-cluster.css, grid-cluster.js

# 4. Проверить manifest
cat dist/manifest.json | grep cdn
# Результат: CDN ссылки

# 5. Проверить CDN ссылки
cat docs/CDN-LINKS.md | head -20
# Результат: готовые ссылки
```

- [ ] Все команды выполнены успешно
- [ ] Нет ошибок
- [ ] Система готова к работе

## 📝 Итоговый статус

- [x] Архитектура спроектирована
- [x] Build скрипты написаны
- [x] GitHub Actions настроен
- [x] Документация подготовлена
- [ ] Первичная настройка выполнена
- [ ] GitHub Action запустился
- [ ] CDN ссылки работают в Carrd
- [ ] Система готова к использованию

---

**Следующий шаг**: Выполните все пункты Этапа 2 для первичной настройки!
