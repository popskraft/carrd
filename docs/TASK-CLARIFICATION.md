# 📋 Уточнение задачи: Новый рабочий процесс

## ✅ Как я понял задачу

### 1️⃣ Папка `/mini/src/` — только для справки

**Текущее состояние:**
```
/mini/src/
├── index.html          ← Финальный вид на сайте
└── assets/
    ├── main.css        ← Существующие стили сайта
    └── main.js         ← Существующие скрипты сайта
```

**Назначение:**
- ✅ Справочник для сравнения кода
- ✅ Помощь при разработке (смотрим HTML структуру, классы, селекторы)
- ❌ НЕ используется для разработки новых кастомизаций
- ❌ НЕ берём оттуда код

**Вывод:** Это просто эталон, как выглядит сайт на продакшене.

---

### 2️⃣ Новый рабочий процесс разработки

#### Было (старый способ):
```
/mini/grid-cluster.html  ← Один файл с CSS + JS вместе
```
Содержимое:
```html
<style>/* CSS код */</style>
<script>/* JS код */</script>
```
Использование: копируем весь файл в Custom Code блок на сайте.

---

#### Стало (новый способ):

**Разработка:**
```
/src/mini/grid-cluster/
├── style.css           ← Только CSS (разработка)
├── script.js           ← Только JS (разработка)
├── index.html          ← Описание (опционально)
└── meta.json           ← Метаданные
```

**Build процесс:**
```
npm run build
    ↓
Минификация CSS → dist/mini/grid-cluster.css
Минификация JS → dist/mini/grid-cluster.js
Генерация CDN ссылок
```

**CDN:**
```
https://cdn.jsdelivr.net/gh/popskraft/carrd@main/dist/mini/grid-cluster.css
https://cdn.jsdelivr.net/gh/popskraft/carrd@main/dist/mini/grid-cluster.js
```

**Использование на сайте:**
```html
<!-- Вставляем в Custom Code блок (HEAD или BODY) -->
<link rel="stylesheet" href="https://cdn.jsdelivr.net/gh/popskraft/carrd@main/dist/mini/grid-cluster.css">
<script src="https://cdn.jsdelivr.net/gh/popskraft/carrd@main/dist/mini/grid-cluster.js"></script>
```

---

### 3️⃣ Custom Code блок на Carrd

**Где находится:**
- Carrd → Settings → Custom Code
- Можно поместить в HEAD или BODY
- Можно поместить в любое место

**Что туда вставляем:**
```html
<!-- Обёртка со ссылками на CDN -->
<link rel="stylesheet" href="https://cdn.jsdelivr.net/gh/popskraft/carrd@main/dist/mini/grid-cluster.css">
<script src="https://cdn.jsdelivr.net/gh/popskraft/carrd@main/dist/mini/grid-cluster.js"></script>
```

**Результат:**
- CSS подтягивается с CDN
- JS подтягивается с CDN
- Кастомизация работает на сайте

---

## 📊 Сравнение подходов

| Параметр | Старый (один файл) | Новый (CSS + JS) |
|----------|-------------------|-----------------|
| **Разработка** | grid-cluster.html | style.css + script.js |
| **Хранение** | В одном файле | В отдельных файлах |
| **Использование** | Копируем весь HTML | Ссылки на CDN |
| **Минификация** | Ручная | Автоматическая |
| **Обновления** | Пересохраняем файл | Пуш → GitHub Action → CDN |
| **Кэширование** | Нет | jsDelivr кэширует |

---

## 🔄 Новый рабочий процесс

### Шаг 1: Разработка
```bash
# Создаём структуру
mkdir -p src/mini/grid-cluster

# Создаём файлы
touch src/mini/grid-cluster/{style.css,script.js,meta.json,index.html}
```

### Шаг 2: Разработка кода
```
src/mini/grid-cluster/style.css   ← Пишем CSS
src/mini/grid-cluster/script.js   ← Пишем JS
```

### Шаг 3: Локальное тестирование
```bash
npm run build
# Результат: dist/mini/grid-cluster.{css,js}
```

### Шаг 4: Коммит и пуш
```bash
git add src/
git commit -m "Add: grid-cluster customization"
git push origin main
```

### Шаг 5: GitHub Action (автоматически)
```
✓ Валидирует структуру
✓ Минифицирует CSS и JS
✓ Генерирует dist/
✓ Генерирует CDN ссылки
```

### Шаг 6: Использование на сайте
```html
<!-- Вставляем в Custom Code блок Carrd -->
<link rel="stylesheet" href="https://cdn.jsdelivr.net/gh/popskraft/carrd@main/dist/mini/grid-cluster.css">
<script src="https://cdn.jsdelivr.net/gh/popskraft/carrd@main/dist/mini/grid-cluster.js"></script>
```

---

## 📁 Структура проекта (уточнённая)

```
/carrd/
├── README.md                    ← Главный файл
├── BREAKPOINTS.md               ← Breakpoints
│
├── docs/                        ← Документация
│   ├── GETTING-STARTED.md
│   ├── ARCHITECTURE.md
│   ├── QUICK-REFERENCE.md
│   └── ...
│
├── .github/
│   ├── scripts/
│   │   ├── build.js             ← Минификация
│   │   └── validate.js          ← Валидация
│   └── workflows/
│       └── build-and-deploy.yml ← GitHub Action
│
├── src/                         ← ИСХОДНИКИ ДЛЯ РАЗРАБОТКИ
│   └── mini/
│       ├── grid-cluster/
│       │   ├── style.css        ← Разработка CSS
│       │   ├── script.js        ← Разработка JS
│       │   ├── index.html       ← Описание (опционально)
│       │   └── meta.json        ← Метаданные
│       └── index.json           ← Индекс проекта
│
├── dist/                        ← ГОТОВЫЕ ФАЙЛЫ (генерируется)
│   └── mini/
│       ├── grid-cluster.css     ← Минифицированный CSS
│       └── grid-cluster.js      ← Минифицированный JS
│
├── mini/                        ← СПРАВОЧНИК (не для разработки!)
│   ├── src/                     ← Эталон сайта
│   │   ├── index.html           ← Как выглядит на сайте
│   │   └── assets/
│   │       ├── main.css         ← Существующие стили
│   │       └── main.js          ← Существующие скрипты
│   └── grid-cluster.html        ← Старый способ (для справки)
│
└── package.json
```

---

## ✨ Ключевые моменты

### ✅ Правильно:
- Разработка в `src/mini/grid-cluster/{style.css,script.js}`
- Смотрим `mini/src/` только для справки (селекторы, классы)
- Используем CDN ссылки на сайте
- Автоматическая минификация и деплой

### ❌ Неправильно:
- Разработка в `mini/grid-cluster.html`
- Копирование кода из `mini/src/`
- Ручная минификация
- Вставка всего HTML в Custom Code

---

## 🎯 Итоговый процесс

```
1. Разработка
   ├── Пишем CSS в src/mini/grid-cluster/style.css
   └── Пишем JS в src/mini/grid-cluster/script.js

2. Тестирование
   └── npm run build → проверяем dist/

3. Деплой
   └── git push origin main

4. GitHub Action (автоматически)
   ├── Валидирует
   ├── Минифицирует
   └── Генерирует CDN ссылки

5. Использование
   └── Вставляем ссылки в Custom Code на Carrd
```

---

## 📝 Уточнения

**Вопрос:** Где находится Custom Code блок?
**Ответ:** Carrd → Settings → Custom Code (можно в HEAD или BODY)

**Вопрос:** Что туда вставляем?
**Ответ:** Две ссылки (CSS и JS) с CDN

**Вопрос:** Как обновить кастомизацию?
**Ответ:** Редактируем src/ → git push → GitHub Action → CDN обновляется

**Вопрос:** Зачем папка mini/src/?
**Ответ:** Только для справки (смотрим структуру, селекторы, классы)

---

## ✅ Готово!

Я понял задачу. Готов начать разработку по новому процессу.

**Следующий шаг:** Создать первую кастомизацию в новой структуре или обновить документацию?
