# 📊 Репорт: Как я понял задачу

## ✅ Основные уточнения

### 1️⃣ Папка `/mini/src/` — только справочник

```
/mini/src/
├── index.html          ← Эталон: как выглядит на сайте
└── assets/
    ├── main.css        ← Существующие стили (для справки)
    └── main.js         ← Существующие скрипты (для справки)
```

**Назначение:**
- ✅ Смотрим структуру HTML
- ✅ Смотрим селекторы и классы
- ✅ Смотрим как работает существующий код
- ❌ НЕ копируем оттуда код
- ❌ НЕ используем для разработки

**Вывод:** Это просто эталон для сравнения.

---

### 2️⃣ Новый способ разработки: CSS + JS отдельно

#### Было (старый способ):
```
/mini/grid-cluster.html  ← Один файл
```
Содержимое:
```html
<style>
  /* CSS код */
</style>
<script>
  /* JS код */
</script>
```
Использование: копируем весь файл в Custom Code на сайте.

---

#### Стало (новый способ):

**Разработка в отдельных файлах:**
```
/src/mini/grid-cluster/
├── style.css           ← Только CSS
├── script.js           ← Только JS
├── index.html          ← Описание (опционально)
└── meta.json           ← Метаданные
```

**Build процесс:**
```
npm run build
    ↓
Минификация CSS → dist/mini/grid-cluster.css
Минификация JS → dist/mini/grid-cluster.js
Генерация CDN ссылок
```

**Использование на сайте:**
```html
<!-- Вставляем в Custom Code блок Carrd -->
<link rel="stylesheet" href="https://cdn.jsdelivr.net/gh/popskraft/carrd@main/dist/mini/grid-cluster.css">
<script src="https://cdn.jsdelivr.net/gh/popskraft/carrd@main/dist/mini/grid-cluster.js"></script>
```

---

### 3️⃣ Custom Code блок на Carrd

**Где находится:**
```
Carrd → Settings → Custom Code
```

**Где можно поместить:**
- В HEAD
- В BODY
- В любое место на странице

**Что туда вставляем:**
```html
<!-- Обёртка со ссылками на CDN -->
<link rel="stylesheet" href="https://cdn.jsdelivr.net/gh/popskraft/carrd@main/dist/mini/grid-cluster.css">
<script src="https://cdn.jsdelivr.net/gh/popskraft/carrd@main/dist/mini/grid-cluster.js"></script>
```

**Результат:**
- CSS подтягивается с CDN
- JS подтягивается с CDN
- Кастомизация работает на сайте

---

## 📊 Сравнение подходов

| Параметр | Старый | Новый |
|----------|--------|-------|
| **Разработка** | grid-cluster.html | style.css + script.js |
| **Хранение** | Один файл | Отдельные файлы |
| **Использование** | Копируем весь HTML | Ссылки на CDN |
| **Минификация** | Ручная | Автоматическая |
| **Обновления** | Пересохраняем файл | git push → GitHub Action → CDN |
| **Кэширование** | Нет | jsDelivr кэширует |
| **Масштабируемость** | Сложно | Легко |

---

## 🔄 Новый рабочий процесс

### Шаг 1: Разработка
```bash
# Создаём структуру
mkdir -p src/mini/grid-cluster

# Создаём файлы
touch src/mini/grid-cluster/{style.css,script.js,meta.json}
```

### Шаг 2: Разработка кода
```
src/mini/grid-cluster/style.css   ← Пишем CSS
src/mini/grid-cluster/script.js   ← Пишем JS
```

**Справочник:**
```
Смотрим mini/src/ для:
- HTML структуры
- Селекторов и классов
- Как работает существующий код
```

### Шаг 3: Локальное тестирование
```bash
npm run build
# Результат: dist/mini/grid-cluster.{css,js}
```

### Шаг 4: Коммит и пуш
```bash
git add src/
git commit -m "Add: grid-cluster customization"
git push origin main
```

### Шаг 5: GitHub Action (автоматически)
```
✓ Валидирует структуру
✓ Минифицирует CSS и JS
✓ Генерирует dist/
✓ Генерирует CDN ссылки
```

### Шаг 6: Использование на сайте
```html
<!-- Вставляем в Custom Code блок Carrd -->
<link rel="stylesheet" href="https://cdn.jsdelivr.net/gh/popskraft/carrd@main/dist/mini/grid-cluster.css">
<script src="https://cdn.jsdelivr.net/gh/popskraft/carrd@main/dist/mini/grid-cluster.js"></script>
```

---

## 📁 Структура проекта (уточнённая)

```
/carrd/
├── README.md                    ← Главный файл
├── BREAKPOINTS.md               ← Breakpoints
│
├── docs/                        ← Документация
│   ├── GETTING-STARTED.md
│   ├── ARCHITECTURE.md
│   └── ...
│
├── .github/
│   ├── scripts/
│   │   ├── build.js             ← Минификация
│   │   └── validate.js          ← Валидация
│   └── workflows/
│       └── build-and-deploy.yml ← GitHub Action
│
├── src/                         ← РАЗРАБОТКА (новые кастомизации)
│   └── mini/
│       ├── grid-cluster/
│       │   ├── style.css        ← Разработка CSS
│       │   ├── script.js        ← Разработка JS
│       │   ├── index.html       ← Описание
│       │   └── meta.json        ← Метаданные
│       └── index.json
│
├── dist/                        ← ГОТОВЫЕ ФАЙЛЫ (генерируется)
│   └── mini/
│       ├── grid-cluster.css     ← Минифицированный CSS
│       └── grid-cluster.js      ← Минифицированный JS
│
├── mini/                        ← СПРАВОЧНИК (не для разработки!)
│   ├── src/                     ← Эталон сайта
│   │   ├── index.html           ← Структура сайта
│   │   └── assets/
│   │       ├── main.css         ← Существующие стили
│   │       └── main.js          ← Существующие скрипты
│   └── grid-cluster.html        ← Старый способ (для справки)
│
└── package.json
```

---

## ✨ Ключевые правила

### ✅ Правильно:
1. Разработка в `src/mini/grid-cluster/{style.css,script.js}`
2. Смотрим `mini/src/` только для справки (селекторы, классы)
3. Используем CDN ссылки на сайте
4. Автоматическая минификация и деплой
5. Каждая кастомизация в отдельной папке

### ❌ Неправильно:
1. Разработка в `mini/grid-cluster.html`
2. Копирование кода из `mini/src/`
3. Ручная минификация
4. Вставка всего HTML в Custom Code
5. Смешивание CSS и JS в одном файле

---

## 🎯 Итоговый процесс

```
1. РАЗРАБОТКА
   ├── Пишем CSS в src/mini/grid-cluster/style.css
   ├── Пишем JS в src/mini/grid-cluster/script.js
   └── Смотрим mini/src/ для справки

2. ТЕСТИРОВАНИЕ
   └── npm run build → проверяем dist/

3. ДЕПЛОЙ
   └── git push origin main

4. GITHUB ACTION (автоматически)
   ├── Валидирует структуру
   ├── Минифицирует CSS и JS
   └── Генерирует CDN ссылки

5. ИСПОЛЬЗОВАНИЕ
   └── Вставляем ссылки в Custom Code на Carrd
```

---

## 📝 Ответы на вопросы

**Q: Где находится Custom Code блок?**
A: Carrd → Settings → Custom Code (можно в HEAD или BODY)

**Q: Что туда вставляем?**
A: Две ссылки (CSS и JS) с CDN

**Q: Как обновить кастомизацию?**
A: Редактируем src/ → git push → GitHub Action → CDN обновляется

**Q: Зачем папка mini/src/?**
A: Только для справки (смотрим структуру, селекторы, классы)

**Q: Можно ли копировать код из mini/src/?**
A: Нет, это только справочник. Пишем свой код в src/

**Q: Как работает минификация?**
A: Автоматически при `npm run build` или при git push

**Q: Где найти CDN ссылки?**
A: В `docs/CDN-LINKS.md` (генерируется автоматически)

---

## ✅ Готово!

Я полностью понял новый рабочий процесс:

1. ✅ `/mini/src/` — только справочник
2. ✅ Разработка в `src/` (CSS и JS отдельно)
3. ✅ Build → GitHub Action → CDN
4. ✅ Использование через Custom Code блок на Carrd

**Готов начать разработку по новому процессу!**
