# Workflow процесс

## 🔄 Полный цикл разработки

```
┌─────────────────────────────────────────────────────────────┐
│                    РАЗРАБОТКА                               │
└─────────────────────────────────────────────────────────────┘

1. Редактируете код
   ↓
   src/mini/grid-cluster/
   ├── style.css      ← Меняете стили
   ├── script.js      ← Меняете скрипты
   └── meta.json      ← Меняете версию

2. Локально тестируете
   ↓
   npm run validate   ← Проверяет структуру
   npm run build      ← Генерирует dist/

3. Проверяете результаты
   ↓
   dist/mini/grid-cluster.css
   dist/mini/grid-cluster.js
   docs/CDN-LINKS.md

4. Коммитите и пушите
   ↓
   git add src/
   git commit -m "Update: grid-cluster"
   git push origin main

┌─────────────────────────────────────────────────────────────┐
│              GITHUB ACTIONS (АВТОМАТИЧЕСКИ)                │
└─────────────────────────────────────────────────────────────┘

5. Триггер: push в main
   ↓
   .github/workflows/build-and-deploy.yml

6. Валидация структуры
   ↓
   .github/scripts/validate.js
   ✓ Проверяет index.json
   ✓ Проверяет meta.json
   ✓ Проверяет CSS и JS файлы

7. Build и минификация
   ↓
   .github/scripts/build.js
   ✓ Минифицирует CSS
   ✓ Минифицирует JS
   ✓ Генерирует dist/manifest.json
   ✓ Генерирует docs/CDN-LINKS.md

8. Коммит результатов
   ↓
   git commit -m "chore: build CDN assets"
   git push origin main

┌─────────────────────────────────────────────────────────────┐
│                    CDN (jsDelivr)                           │
└─────────────────────────────────────────────────────────────┘

9. jsDelivr кэширует файлы
   ↓
   https://cdn.jsdelivr.net/gh/popskraft/carrd@main/dist/mini/grid-cluster.css
   https://cdn.jsdelivr.net/gh/popskraft/carrd@main/dist/mini/grid-cluster.js

┌─────────────────────────────────────────────────────────────┐
│                    CARRD WEBSITE                            │
└─────────────────────────────────────────────────────────────┘

10. Используете в Carrd
    ↓
    Custom Code:
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/...">
    <script src="https://cdn.jsdelivr.net/..."></script>
```

## 📊 Структура данных

```
src/mini/grid-cluster/
│
├── index.html
│   └── Описание для документации
│
├── style.css
│   └── Исходные стили (с комментариями)
│
├── script.js
│   └── Исходные скрипты (с комментариями)
│
└── meta.json
    └── Метаданные (версия, описание, зависимости)
         │
         ├── name: "grid-cluster"
         ├── version: "1.0.0"
         ├── description: "..."
         ├── files: { css: "style.css", js: "script.js" }
         └── changelog: { "1.0.0": "Initial release" }

                    ↓ BUILD PROCESS ↓

dist/mini/
│
├── grid-cluster.css
│   └── Минифицированный CSS (без комментариев)
│
├── grid-cluster.js
│   └── Минифицированный JS (без комментариев)
│
└── manifest.json
    └── Индекс всех файлов с CDN ссылками
         │
         ├── version: "1.0.0"
         ├── generated: "2024-11-11T..."
         └── projects: [
               {
                 "project": "mini",
                 "customizations": [
                   {
                     "id": "grid-cluster",
                     "version": "1.0.0",
                     "cdn": {
                       "css": "https://cdn.jsdelivr.net/...",
                       "js": "https://cdn.jsdelivr.net/..."
                     }
                   }
                 ]
               }
             ]

                    ↓ DOCUMENTATION ↓

docs/CDN-LINKS.md
└── Все CDN ссылки для копирования в Carrd
```

## 🔀 Параллельные процессы

```
┌─────────────────────────────────────────────────────────────┐
│ Разработчик                    │ GitHub Actions              │
├────────────────────────────────┼─────────────────────────────┤
│ 1. Редактирует код             │                             │
│ 2. npm run build               │                             │
│ 3. Проверяет результаты        │                             │
│ 4. git push                    │                             │
│                                │ 5. Триггер: push           │
│                                │ 6. Валидация               │
│                                │ 7. Build                   │
│                                │ 8. Коммит результатов      │
│                                │ 9. Push dist/              │
│ 10. Проверяет dist/            │                             │
│ 11. Копирует CDN ссылки        │                             │
│ 12. Вставляет в Carrd          │                             │
└────────────────────────────────┴─────────────────────────────┘
```

## ⏱️ Временная шкала

```
t=0s    → git push origin main
t=5s    → GitHub Action триггер
t=10s   → Валидация структуры
t=15s   → Build и минификация
t=20s   → Генерация manifest.json
t=25s   → Коммит и push dist/
t=30s   → GitHub Action завершён
t=60s   → jsDelivr кэширует файлы
t=120s  → CDN ссылки готовы
```

## 🎯 Точки интеграции

### Локальная разработка
```bash
npm run validate    # Проверка перед коммитом
npm run build       # Генерация dist/
npm run dev         # Валидация + build
```

### GitHub Actions
```yaml
- Триггер: push в src/
- Валидация структуры
- Build и минификация
- Коммит результатов
```

### jsDelivr CDN
```
https://cdn.jsdelivr.net/gh/popskraft/carrd@main/dist/{project}/{name}.css
https://cdn.jsdelivr.net/gh/popskraft/carrd@main/dist/{project}/{name}.js
```

### Carrd Website
```html
<link rel="stylesheet" href="https://cdn.jsdelivr.net/...">
<script src="https://cdn.jsdelivr.net/..."></script>
```

## 🔐 Безопасность

```
src/ (исходники)
  ↓ (защищено от прямого доступа)
dist/ (готовые файлы)
  ↓ (коммитится в git)
GitHub
  ↓ (публичный репозиторий)
jsDelivr CDN
  ↓ (глобальное распределение)
Carrd Website
  ↓ (конечный пользователь)
```

## 📈 Масштабируемость

```
1 проект (mini)
├── 1 кастомизация (grid-cluster)
│   ├── style.css
│   └── script.js
│
2 проекта (mini + another-project)
├── mini
│   ├── grid-cluster
│   ├── smooth-scroll
│   └── modal-popup
│
└── another-project
    ├── custom-1
    └── custom-2

N проектов
├── project-1
│   ├── custom-1
│   ├── custom-2
│   └── custom-N
│
└── project-N
    └── custom-N
```

Все автоматически обрабатываются одним build скриптом!
