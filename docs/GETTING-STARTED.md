# Начало работы

## 🎯 Цель

Автоматический деплой кастомизаций на jsDelivr CDN с минификацией и валидацией.

## 📋 Что было создано

### Новая структура
```
src/mini/grid-cluster/
├── index.html      # Описание
├── style.css       # Стили
├── script.js       # Скрипты
└── meta.json       # Метаданные
```

### Build скрипты
- `.github/scripts/build.js` — основной build скрипт
- `.github/scripts/validate.js` — валидация структуры
- `.github/workflows/build-and-deploy.yml` — GitHub Action

### Документация
- `docs/ARCHITECTURE.md` — архитектура
- `docs/SETUP.md` — первичная настройка
- `MIGRATION.md` — миграция со старой структуры

## 🚀 Первичная настройка (один раз)

### 1. Установить зависимости
```bash
cd /Users/popskraft/projects/carrd
npm install
```

### 2. Валидировать структуру
```bash
npm run validate
```

Должно вывести:
```
✓ Validation passed!
```

### 3. Локальный build
```bash
npm run build
```

Должно создать:
```
dist/
├── mini/
│   ├── grid-cluster.css
│   └── grid-cluster.js
├── manifest.json
```

### 4. Проверить результаты
```bash
# Посмотреть CDN ссылки
cat docs/CDN-LINKS.md

# Посмотреть manifest
cat dist/manifest.json
```

### 5. Коммит и пуш
```bash
git add .
git commit -m "Setup: New CDN deployment architecture"
git push origin main
```

### 6. Проверить GitHub Actions
1. Откройте https://github.com/popskraft/carrd/actions
2. Дождитесь завершения workflow `Build and Deploy to CDN`
3. Проверьте `dist/` и `docs/CDN-LINKS.md`

## 📝 Использование в Carrd

После первого успешного build:

1. Откройте `docs/CDN-LINKS.md`
2. Скопируйте нужные ссылки:
   ```html
   <link rel="stylesheet" href="https://cdn.jsdelivr.net/gh/popskraft/carrd@main/dist/mini/grid-cluster.css">
   <script src="https://cdn.jsdelivr.net/gh/popskraft/carrd@main/dist/mini/grid-cluster.js"></script>
   ```
3. Вставьте в Custom Code в Carrd

## 🔄 Рабочий процесс

### Редактирование кода

1. Отредактируйте файлы в `src/mini/grid-cluster/`:
   - `style.css` — стили
   - `script.js` — скрипты

2. Локально протестируйте:
   ```bash
   npm run build
   ```

3. Сделайте коммит:
   ```bash
   git add src/
   git commit -m "Update: grid-cluster improvements"
   git push origin main
   ```

4. GitHub Action автоматически:
   - Валидирует структуру
   - Минифицирует код
   - Обновляет `dist/`
   - Генерирует новые CDN ссылки

5. CDN обновится в течение нескольких минут

### Добавление новой кастомизации

1. Создайте папку:
   ```bash
   mkdir -p src/mini/smooth-scroll
   ```

2. Добавьте файлы:
   - `index.html` — описание
   - `style.css` — стили
   - `script.js` — скрипты
   - `meta.json` — метаданные

3. Обновите `src/mini/index.json`:
   ```json
   {
     "customizations": [
       {
         "id": "smooth-scroll",
         "path": "smooth-scroll",
         "enabled": true
       }
     ]
   }
   ```

4. Сделайте `git push`

5. GitHub Action создаст CDN ссылки

## 📚 Документация

- **[README.md](README.md)** — Обзор проекта
- **[ARCHITECTURE.md](docs/ARCHITECTURE.md)** — Архитектура и структура
- **[SETUP.md](docs/SETUP.md)** — Детальная настройка
- **[MIGRATION.md](MIGRATION.md)** — Миграция со старой структуры
- **[CDN-LINKS.md](docs/CDN-LINKS.md)** — Все CDN ссылки (генерируется)

## 🛠️ Команды

```bash
npm run validate    # Проверить структуру
npm run build       # Build и генерировать manifest
npm run dev         # Валидация + build
```

## ✅ Чек-лист

- [ ] Установлены зависимости (`npm install`)
- [ ] Валидация прошла успешно (`npm run validate`)
- [ ] Локальный build работает (`npm run build`)
- [ ] Файлы созданы в `dist/`
- [ ] CDN ссылки в `docs/CDN-LINKS.md`
- [ ] Коммит и пуш сделаны
- [ ] GitHub Action завершился успешно
- [ ] CDN ссылки работают в Carrd

## 🆘 Решение проблем

### Ошибка: "Missing meta.json"
```bash
# Проверьте структуру
ls -la src/mini/grid-cluster/
# Должны быть: index.html, style.css, script.js, meta.json
```

### Ошибка: "Missing CSS/JS file"
```bash
# Проверьте пути в meta.json
cat src/mini/grid-cluster/meta.json
# Убедитесь, что files.css и files.js указывают на существующие файлы
```

### CDN ссылки не обновляются
1. Дождитесь завершения GitHub Action
2. Очистите кэш: добавьте `?v=1.0.1` к ссылке
3. Проверьте `docs/CDN-LINKS.md`

### GitHub Action не запускается
1. Проверьте `.github/workflows/build-and-deploy.yml`
2. Убедитесь, что файлы в `src/` изменились
3. Проверьте логи в Actions

## 📞 Поддержка

Все ошибки будут видны в:
- **Локально**: консоль при `npm run build`
- **GitHub**: Actions → Build and Deploy to CDN → Logs

## 🎉 Готово!

Ваша система автоматического деплоя на jsDelivr CDN настроена и готова к работе!

Начните с редактирования файлов в `src/mini/grid-cluster/` и делайте `git push`.
